name: CI

on:
  push:
    # Only run on push to main for post-merge verification
    # Feature branches are tested via pull_request events to avoid duplication
    branches: [main]
  pull_request:
    # Runs automatically on all PRs to main (no approval needed for CI steps)
    # This covers all feature branches, avoiding duplicate runs from push events
    branches: [main]
  workflow_dispatch:  # Allows manual trigger for testing or re-running CI

# Concurrency control: Cancel in-progress runs for the same workflow
# Use PR number for PRs, branch name for pushes to prevent duplicate runs
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

# Global environment variables with semantic names
env:
  # Python version configuration
  SUPPORTED_PYTHON_VERSIONS: '["3.11", "3.12"]'
  RELEASE_PYTHON_VERSION: "3.12"
  NODE_VERSION: "24"  # Node.js 24 LTS (Active until 2027-04-30, maintenance until 2030-04-30)
  NPM_VERSION: "11.8.0"  # npm 11.8.0 (compatible with Node.js 24)
  
  # Cache configuration
  CACHE_WHISPER_MODELS: true
  ARTIFACT_RETENTION_DAYS: 90
  
  # Test configuration
  RUN_INTEGRATION_TESTS_ON_PR: false
  RUN_INTEGRATION_TESTS_ON_MAIN: true
  
  # Whisper model cache location
  XDG_CACHE_HOME: ${{ github.workspace }}/.cache

# Default permissions (least privilege)
permissions:
  contents: read

jobs:
  # Backend linting - runs fast with fail-fast enabled
  backend-lint:
    name: Backend Lint (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest  # Using latest to get security updates; pin to ubuntu-22.04 or ubuntu-24.04 if regressions occur
    strategy:
      fail-fast: true
      matrix:
        # Lint on multiple Python versions to catch version-specific syntax issues
        # Uses hardcoded JSON to avoid dependency on repository variables
        python-version: ${{ fromJSON('["3.11", "3.12"]') }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Cache UV dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/uv
            ~/.local/share/uv
          key: uv-lint-${{ runner.os }}-py${{ matrix.python-version }}-${{ hashFiles('backend/uv.lock') }}-${{ hashFiles('backend/pyproject.toml') }}
          restore-keys: |
            uv-lint-${{ runner.os }}-py${{ matrix.python-version }}-${{ hashFiles('backend/uv.lock') }}-
            uv-lint-${{ runner.os }}-py${{ matrix.python-version }}-

      - name: Install dependencies
        working-directory: backend
        run: |
          uv sync --dev

      - name: Run Ruff format and lint
        uses: ./.github/actions/ruff
        with:
          working-directory: backend

      - name: Pyright type check
        working-directory: backend
        run: |
          uv run pyright

  # Backend unit tests - runs after lint passes (single Python version for efficiency and stable CI)
  backend-test-unit:
    name: Backend Unit Tests (Python 3.12)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: backend-lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python ${{ env.RELEASE_PYTHON_VERSION }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ env.RELEASE_PYTHON_VERSION }}

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Cache UV dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/uv
            ~/.local/share/uv
          key: uv-test-${{ runner.os }}-py${{ env.RELEASE_PYTHON_VERSION }}-${{ hashFiles('backend/uv.lock') }}-${{ hashFiles('backend/pyproject.toml') }}
          restore-keys: |
            uv-test-${{ runner.os }}-py${{ env.RELEASE_PYTHON_VERSION }}-${{ hashFiles('backend/uv.lock') }}-
            uv-test-${{ runner.os }}-py${{ env.RELEASE_PYTHON_VERSION }}-

      - name: Cache Whisper models
        if: env.CACHE_WHISPER_MODELS == 'true'
        uses: actions/cache@v5
        with:
          path: |
            ${{ github.workspace }}/.cache/whisper
          key: whisper-${{ runner.os }}-base
          restore-keys: |
            whisper-${{ runner.os }}-

      - name: Install dependencies
        working-directory: backend
        run: |
          uv sync --dev

      - name: Cache pytest cache
        uses: actions/cache@v5
        with:
          path: backend/.pytest_cache
          key: pytest-${{ runner.os }}-py${{ env.RELEASE_PYTHON_VERSION }}-${{ hashFiles('backend/tests/**/*.py') }}
          restore-keys: |
            pytest-${{ runner.os }}-py${{ env.RELEASE_PYTHON_VERSION }}-

      - name: Run unit tests
        working-directory: backend
        run: |
          # Sequential run with verbose output for progress tracking
          # (parallelization with -n causes pytest-xdist to hang in CI environment)
          uv run pytest tests/test_unit.py -vv --tb=short

  # Frontend linting and tests
  frontend-test:
    name: Frontend Lint & Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install npm ${{ env.NPM_VERSION }}
        run: npm install -g npm@${{ env.NPM_VERSION }}

      - name: Install dependencies
        working-directory: frontend
        run: npm ci --legacy-peer-deps

      - name: Run ESLint
        working-directory: frontend
        run: npm run lint

      - name: Check formatting
        working-directory: frontend
        run: npm run format -- --check

      - name: Run tests
        working-directory: frontend
        run: npm run test

      - name: Build
        working-directory: frontend
        run: npm run build

  # Markdown checks
  markdown-check:
    name: Markdown Checks
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ env.RELEASE_PYTHON_VERSION }}

      - name: Install pre-commit
        run: python -m pip install --upgrade pip pre-commit

      - name: Run merge-conflict marker check
        run: pre-commit run check-merge-conflict --all-files

      - name: Run local Markdown link target validation
        run: pre-commit run check-markdown-local-links --all-files

      - name: Run Markdown linting
        run: pre-commit run markdownlint-cli2 --all-files

      - name: Run Markdown link/reference validation
        run: pre-commit run remark-validate-links --all-files

  # CI Summary job - aggregates all results
  ci-summary:
    name: CI Summary
    runs-on: ubuntu-latest
    needs: [backend-lint, backend-test-unit, frontend-test, markdown-check]
    if: always()
    permissions:
      contents: read
    steps:
      - name: Generate CI Summary
        run: |
          echo "# CI Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Job Status" >> $GITHUB_STEP_SUMMARY
          echo "| Job | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-----|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Backend Lint | ${{ needs.backend-lint.result == 'success' && '✅ Passed' || needs.backend-lint.result == 'skipped' && '⏭️ Skipped' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Backend Unit Tests | ${{ needs.backend-test-unit.result == 'success' && '✅ Passed' || needs.backend-test-unit.result == 'skipped' && '⏭️ Skipped' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Frontend Lint & Test | ${{ needs.frontend-test.result == 'success' && '✅ Passed' || needs.frontend-test.result == 'skipped' && '⏭️ Skipped' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Markdown Checks | ${{ needs.markdown-check.result == 'success' && '✅ Passed' || needs.markdown-check.result == 'skipped' && '⏭️ Skipped' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Overall status
          if [ "${{ needs.backend-lint.result }}" == "success" ] && \
             [ "${{ needs.backend-test-unit.result }}" == "success" ] && \
             [ "${{ needs.frontend-test.result }}" == "success" ] && \
             [ "${{ needs.markdown-check.result }}" == "success" ]; then
            echo "✅ **All checks passed - ready for review!**" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **Some checks failed - please review the logs above.**" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Configuration" >> $GITHUB_STEP_SUMMARY
          echo "- Python versions: ${{ env.SUPPORTED_PYTHON_VERSIONS }}" >> $GITHUB_STEP_SUMMARY
          echo "- Node.js version: ${{ env.NODE_VERSION }}" >> $GITHUB_STEP_SUMMARY
          echo "- Note: Integration tests run separately in pre-release workflow" >> $GITHUB_STEP_SUMMARY

      - name: Check overall status
        if: |
          needs.backend-lint.result != 'success' ||
          needs.backend-test-unit.result != 'success' ||
          needs.frontend-test.result != 'success' ||
          needs.markdown-check.result != 'success'
        run: |
          echo "One or more jobs failed"
          exit 1
