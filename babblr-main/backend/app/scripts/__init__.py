"""Script entry points for local development and diagnostics.

These modules are intended to be executed via installed console scripts
defined in `backend/pyproject.toml` (for example with `uv run ...`).
"""
